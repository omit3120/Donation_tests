Автоматизированные тесты для страницы донатов на [FundraiseUp QA Test](https://data.fundraiseup.com/qa-test-7R58U3/), написанные с использованием **Python**, **Playwright** и **Pytest**.  

Тесты покрывают:
- Проверку кнопки **Give Now** и появление формы **Secure Donation**  
- Переход на вкладку **Monthly**  
- Проверку кнопки **Donate with other methods**  
- Проверку полей формы **Enter your details**

---

## 📁 Структура проекта

\`\`\`
donation-tests/
│
├── tests/                  # Тестовые сценарии
│   └── test_donation_flow.py
├── pages/                  # Page Object модели
│   ├── base_page.py
│   └── donation_page.py
├── utils/                  # Вспомогательные функции и тестовые данные
│   └── test_data.py
├── conftest.py             # Фикстуры Pytest (браузер, страницы)
├── requirements.txt        # Зависимости проекта
├── pytest.ini              # Настройки Pytest
└── README.md               # Этот файл
\`\`\`

---

## ⚙️ Установка и подготовка

1. Клонируем репозиторий:
\`\`\`bash
git clone https://github.com/omit3120/Donation_tests
cd donation-tests
\`\`\`

2. Создаём виртуальное окружение:
\`\`\`bash
python -m venv venv
source venv/bin/activate   # Linux/macOS
venv\\Scripts\\activate    # Windows
\`\`\`

3. Устанавливаем зависимости:
\`\`\`bash
pip install -r requirements.txt
\`\`\`

4. Устанавливаем Playwright браузеры:
\`\`\`bash
playwright install
\`\`\`

---

## 🚀 Запуск тестов

### Смоук тесты:
\`\`\`bash
pytest -v -m smoke
\`\`\`

### E2E тесты:
\`\`\`bash
pytest -v -m e2e
\`\`\`

### С Allure отчетами:
\`\`\`bash
pytest -v --alluredir=allure-results
allure serve allure-results
\`\`\`

---

## 🧩 Фикстуры

В \`conftest.py\` настроены:
- \`page\` — создаёт Playwright страницу/браузер  
- \`donation_page\` — объект \`DonationPage\` для работы с кнопками и формами
